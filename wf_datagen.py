"""
(Tentative) Contains the code for fetching or suimulating data for the project
Saves raw data in the data_original folder
"""

# import requests

# url = "your_data_source"
# response = requests.get(url)
# with open("data_original/your_file.csv", "w") as f:
#     f.write(response.text)
